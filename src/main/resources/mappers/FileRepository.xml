<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--이 XML이 어떤 자바 인터페이스와 짝인지 지정. FileRepository 인터페이스의 풀 경로를 그대로 적음 -->
<mapper namespace="com.study.file.repository.FileRepository">
  <!-- 첨부파일 등록 -->
  <!-- useGeneratedKeys="true" DB가 자동 생성한 키(AUTO_INCREMENT 값)를 가져오기 -->
  <!-- keyProperty="id" 가져온 그 값을 파라미터로 넘긴 객체의 id 필드에 넣어주기 -->
  <insert id="insertFile" parameterType="com.study.file.entity.FileEntity"
    useGeneratedKeys="true" keyProperty="id">
    INSERT INTO files (board_id, origin_name, stored_name, file_path, file_size, file_format)
    VALUES (#{boardId}, #{originName}, #{storedName}, #{filePath}, #{fileSize}, #{fileFormat})
  </insert>

  <!-- 게시글별 첨부파일 목록 조회 -->
  <select id="selectFilesByBoardId" resultType="com.study.file.entity.FileEntity">
    SELECT id, board_id, origin_name, stored_name, file_path, file_size, file_format
    FROM files
    WHERE board_id = #{boardId}
  </select>

  <!-- 파일 id로 상세 조회 -->
  <select id="selectFileById" resultType="com.study.file.entity.FileEntity">
    SELECT id, board_id, origin_name, stored_name, file_path, file_size, file_format
    FROM files
    WHERE id = #{id}
  </select>
</mapper>
