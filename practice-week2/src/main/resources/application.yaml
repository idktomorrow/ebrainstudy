spring:
  datasource:
    url: jdbc:mysql://localhost:3309/practice_week2?useUnicode=true&characterEncoding=UTF-8
    username: practice
    password: practice
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

mybatis:
  mapper-locations: classpath:mappers/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

app:
  upload-dir: ./uploads   # 첨부파일이 저장될 로컬 디렉토리 (프로젝트 루트 기준 상대경로)
