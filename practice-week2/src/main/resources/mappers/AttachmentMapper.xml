<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study2.practice.file.mapper.AttachmentMapper">

  <!-- 첨부파일 메타데이터 등록 -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO files (board_id, origin_name, stored_name, file_path, file_size, file_format)
    VALUES (#{boardId}, #{originName}, #{storedName}, #{filePath}, #{fileSize}, #{fileFormat})
  </insert>

  <!-- 게시글별 첨부파일 목록 -->
  <select id="findByBoardId" resultType="com.study2.practice.file.entity.Attachment">
    SELECT id, board_id, origin_name, stored_name, file_path, file_size, file_format
    FROM files
    WHERE board_id = #{boardId}
  </select>

  <!-- 첨부파일 단건 조회 -->
  <select id="findById" resultType="com.study2.practice.file.entity.Attachment">
    SELECT id, board_id, origin_name, stored_name, file_path, file_size, file_format
    FROM files
    WHERE id = #{id}
  </select>

  <!-- 첨부파일 메타데이터 삭제 -->
  <delete id="delete">
    DELETE FROM files
    WHERE id = #{id}
  </delete>

</mapper>
